// Online C compiler to run C program online
#include <stdio.h>

int main() {
    // Write C code here);
    int num;
    printf("enter the number:");
    scanf("%d",&num);
    if(num%5==0 && num%11==0)
    printf("number is divisible by both 5 and 11");
    else
    printf("number is not divible by both  5 and 11");
    
    

    return 0;
}
